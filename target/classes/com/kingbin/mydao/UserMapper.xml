<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kingbin.mydao.UserMapper">

    <select id="findUserByUser" parameterType="com.kingbin.bean.UserBean" resultType="com.kingbin.bean.UserBean">
        select * from user
        <where>
            <if test="username != null">and username = #{username,jdbcType=VARCHAR}</if>
            <if test="age != null">and age = #{age,jdbcType=INTEGER}</if>
        </where>
    </select>

    <update id="updateUser" parameterType="com.kingbin.bean.UserBean">
        update user
        <trim prefix="set" suffixOverrides=",">
            <if test="username!=null">username = #{username,jdbcType = VARCHAR},</if>
            <if test="password!=null">password = #{password,jdbcType = VARCHAR},</if>
            <if test="age!=null">age = #{age,jdbcType = INTEGER},</if>
        </trim>
        where id = #{id,jdbcType = BIGINT}
    </update>
</mapper>